{
	"name": "@minerva/gateway",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"build": "vite build",
		"preview": "bun run build && vite preview",
		"types": "wrangler types -c wrangler.jsonc -c ../services/auth/wrangler.jsonc",
		"codecheck": "biome check --write"
	},
	"devDependencies": {
		"hono": "^4.7.10"
	}
}
