{
	"name": "@minerva/auth",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"deploy": "wrangler deploy --minify",
		"types": "wrangler types --include-runtime false --env-interface AuthEnv",
		"typecheck": "tsc",
		"codecheck": "biome check --write"
	}
}
