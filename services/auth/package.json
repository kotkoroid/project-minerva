{
	"name": "auth",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"deploy": "wrangler deploy --minify",
		"types": "wrangler types",
		"typecheck": "tsc --noEmit",
		"codecheck": "biome check --write"
	}
}
